// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.shekhar.playground.roo.poll.domain;

import com.shekhar.playground.roo.poll.domain.Answer;
import java.util.List;
import java.util.Random;
import org.springframework.stereotype.Component;

privileged aspect AnswerDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AnswerDataOnDemand: @Component;
    
    private Random AnswerDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Answer> AnswerDataOnDemand.data;
    
    public Answer AnswerDataOnDemand.getNewTransientAnswer(int index) {
        com.shekhar.playground.roo.poll.domain.Answer obj = new com.shekhar.playground.roo.poll.domain.Answer();
        obj.setAnswer("answer_" + index);
        return obj;
    }
    
    public Answer AnswerDataOnDemand.getSpecificAnswer(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Answer obj = data.get(index);
        return Answer.findAnswer(obj.getId());
    }
    
    public Answer AnswerDataOnDemand.getRandomAnswer() {
        init();
        Answer obj = data.get(rnd.nextInt(data.size()));
        return Answer.findAnswer(obj.getId());
    }
    
    public boolean AnswerDataOnDemand.modifyAnswer(Answer obj) {
        return false;
    }
    
    public void AnswerDataOnDemand.init() {
        data = com.shekhar.playground.roo.poll.domain.Answer.findAnswerEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Answer' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<com.shekhar.playground.roo.poll.domain.Answer>();
        for (int i = 0; i < 10; i++) {
            com.shekhar.playground.roo.poll.domain.Answer obj = getNewTransientAnswer(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
